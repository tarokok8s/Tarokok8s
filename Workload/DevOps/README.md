# DevOps

```bash
git clone https://github.com/tarokok8s/Tarokok8s.git && sudo rm -rf .git && mv Tarokok8s/Workload/DevOps/ ~/devops
cd ~/devops && git init && git add -A && git commit -m "init repo"

```